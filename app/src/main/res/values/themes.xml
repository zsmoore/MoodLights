<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoodLights" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gruv_light_primary</item>
        <item name="colorOnPrimary">@color/gruv_light_foreground</item>

        <item name="android:textColorPrimary">@color/gruv_light_foreground</item>
        <item name="android:windowBackground">@color/gruv_light_bg</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>
</resources>